#include <stdio.h>
/**
 * main() - an infinite loop
 * return - 0
 */
int main(void)
{
	int i;

	printf("infinte loop :(\n");

	i = 0;
	/**
	 * while (i < 10)
	 * {
	 * 	putchar(i);
	 * }
	 */

	printf("infinite lop avoided \\o/\n");

	return 0;
}
